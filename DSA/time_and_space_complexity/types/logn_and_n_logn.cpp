#include <iostream>

using namespace std;

int main() {
  // logn is an amazing time complexity as it is a small value. It resides close to O(1) in graph. It is better than O(n).
  // binary search is a time complexity of O(logn)

  // n logn lies between O(n) and O(n^2)
  cout << "Hello World!" << endl;
}
